{
  "name": "app-tm",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only --ignore-watch node_modules --no-notify backend/src/index.ts",
    "dev:nodemon": "nodemon --config nodemon.json",
    "server:legacy": "node backend/server.js",
    "build": "tsc -b backend",
    "start": "node backend/dist/index.js",
    "db:test": "ts-node backend/src/scripts/test-db-connection.ts",
    "clean": "rimraf backend/dist"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "pg": "^8.11.5",
    "reflect-metadata": "^0.1.13",
    "typeorm": "^0.3.20"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "nodemon": "^3.1.0",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.4.0",
    "rimraf": "^5.0.5"
  }
}
